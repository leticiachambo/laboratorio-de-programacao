# API de inferência

A ser desenvolvida na Unidade 2: endpoint(s) para receber uma imagem,
executar a inferência do modelo de detecção treinado e retornar raça,
confiança e bounding box, além de persistir o resultado via `database/schema.sql`.
